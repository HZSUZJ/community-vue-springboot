<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.su.community.mapper.UserMapper">
    <insert id="addUser" parameterType="User">
        insert into user (account_id, name, token, gmt_create, gmt_modified, avatar_url)
        values (#{account_id}, #{name}, #{token}, #{gmt_create}, #{gmt_modified}, #{avatarUrl})
    </insert>

    <select id="findByToken" parameterType="String" resultType="User">
        select *
        from user
        where token = #{token}
    </select>

    <select id="findById" resultType="User">
        select *
        from user
        where id = #{id}
    </select>

    <select id="findByAccountId" parameterType="String" resultType="User">
        select *
        from user
        where account_id = #{accountId}
    </select>

    <update id="update" parameterType="User">
        update user
        set name        = #{name},
            token=#{token},
            gmt_modified=#{gmt_modified},
            avatar_url=#{avatarUrl}
        where id = #{id}
    </update>

    <select id="findByIds" resultType="User" parameterType="list">
        select *
        from user
        where id in
        <foreach collection="ids" item="id" index="index" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </select>
</mapper>