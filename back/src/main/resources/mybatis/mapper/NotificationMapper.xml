<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.su.community.mapper.NotificationMapper">
    <insert id="insert" parameterType="Notification">
        insert into notification (id, notifier, receiver,outerId, type, gmt_create,status)
        values (#{id}, #{notifier}, #{receiver}, #{outerId}, #{type}, #{gmtCreate}, #{status})
    </insert>
</mapper>